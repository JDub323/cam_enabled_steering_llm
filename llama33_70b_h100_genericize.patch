diff --git a/README.md b/README.md
index b28ffd5..626415a 100644
--- a/README.md
+++ b/README.md
@@ -1,11 +1,11 @@
-# Gemma 2 Steering Research
+# Llama 3.3 70B Steering Research
 
-A small repo for residual-stream steering experiments on `google/gemma-2-2b` using Hugging Face Transformers and `lm-evaluation-harness`.
+A small repo for residual-stream steering experiments on `meta-llama/Llama-3.3-70B-Instruct` using Hugging Face Transformers and `lm-evaluation-harness`.
 
 The core idea is intentionally narrow:
 
-1. Load Gemma 2 with Hugging Face Transformers.
-2. Wrap it with `SteeredGemma2`.
+1. Load Llama 3.3 70B Instruct with Hugging Face Transformers.
+2. Wrap it with `SteeredCausalLM`.
 3. Attach PyTorch forward hooks at configurable decoder-layer points.
 4. Extract the batched last-token residual stream at the classification hook.
 5. Send that activation to a `Steerer`.
@@ -19,8 +19,8 @@ This repo is meant to stay small, readable, and easy to modify for interpretabil
 ## Repository layout
 
 ```text
-gemma2-steering-research/
-├── gemma2_steering/
+llm-steering-research/
+├── llm_steering/
 │   ├── __init__.py
 │   ├── lm_eval.py
 │   ├── model.py
@@ -37,15 +37,15 @@ gemma2-steering-research/
 
 ### Important files
 
-`gemma2_steering/steerers.py` defines the steering interface and the included `ZeroSteerer` and `ConstantSteerer` classes.
+`llm_steering/steerers.py` defines the steering interface and the included `ZeroSteerer` and `ConstantSteerer` classes.
 
-`gemma2_steering/model.py` defines `SteeredGemma2`, the wrapper that attaches hooks to Gemma 2.
+`llm_steering/model.py` defines `SteeredCausalLM`, the wrapper that attaches hooks to decoder-only Hugging Face causal language models.
 
-`gemma2_steering/lm_eval.py` contains the adapter for using the steered model with `lm-evaluation-harness`.
+`llm_steering/lm_eval.py` contains the adapter for using the steered model with `lm-evaluation-harness`.
 
 `benchmarks/` contains repo-owned benchmark definitions, including the local sycophancy task YAML.
 
-`notebooks/gemma2_benchmarks.ipynb` shows how to install the repo, load Gemma 2, create steerers, wrap the model, and run benchmark cells.
+`notebooks/gemma2_benchmarks.ipynb` shows how to install the repo, load Llama 3.3 70B Instruct, create steerers, wrap the model, and run benchmark cells.
 
 ---
 
@@ -57,7 +57,7 @@ Install the repo in editable mode:
 pip install -e ".[eval,dev]"
 ```
 
-Gemma 2 is gated on Hugging Face, so you will need to have accepted the model license and logged in with a Hugging Face token before loading the model.
+Llama 3.3 70B Instruct is gated on Hugging Face, so you will need to have accepted the model license and logged in with a Hugging Face token before loading the model.
 
 ```python
 from huggingface_hub import login
@@ -71,12 +71,12 @@ login()
 
 ```python
 import torch
-from gemma2_steering import SteeredGemma2, ZeroSteerer
+from llm_steering import SteeredCausalLM, ZeroSteerer
 
-model = SteeredGemma2(
+model = SteeredCausalLM(
     ZeroSteerer(),
-    model_name="google/gemma-2-2b",
-    layer=12,
+    model_name="meta-llama/Llama-3.3-70B-Instruct",
+    layer=40,
     hook_point="layer_output",
     device_map="auto",
     torch_dtype=torch.bfloat16,
@@ -103,7 +103,7 @@ A `Steerer` must implement exactly one method:
 get_steering_vector(self, activation: torch.Tensor) -> torch.Tensor
 ```
 
-The `activation` is the last-token residual stream from the selected Gemma 2 classification layer.
+The `activation` is the last-token residual stream from the selected classification layer.
 
 The shape is always batched:
 
@@ -135,7 +135,7 @@ The repo intentionally includes only two simple steerers.
 `ZeroSteerer` is a no-op baseline.
 
 ```python
-from gemma2_steering import ZeroSteerer
+from llm_steering import ZeroSteerer
 
 steerer = ZeroSteerer()
 ```
@@ -154,9 +154,9 @@ Use this when you want to test the wrapper, hook, notebook, or benchmark pipelin
 
 ```python
 import torch
-from gemma2_steering import ConstantSteerer
+from llm_steering import ConstantSteerer
 
-hidden_size = 2304  # Gemma 2 2B hidden size
+hidden_size = 8192  # Llama 3.3 70B hidden size
 vector = torch.zeros(1, hidden_size)
 
 steerer = ConstantSteerer(vector)
@@ -189,7 +189,7 @@ To make a new steering method, subclass `Steerer` and implement `get_steering_ve
 
 ```python
 import torch
-from gemma2_steering import Steerer
+from llm_steering import Steerer
 
 class MySteerer(Steerer):
     def get_steering_vector(self, activation: torch.Tensor) -> torch.Tensor:
@@ -204,7 +204,7 @@ A slightly more useful example:
 
 ```python
 import torch
-from gemma2_steering import Steerer
+from llm_steering import Steerer
 
 class ScaledDirectionSteerer(Steerer):
     def __init__(self, direction: torch.Tensor, scale: float):
@@ -230,10 +230,10 @@ You could then use it like this:
 direction = torch.randn(1, model.config.hidden_size)
 steerer = ScaledDirectionSteerer(direction, scale=2.0)
 
-steered_model = SteeredGemma2(
+steered_model = SteeredCausalLM(
     steerer,
-    model_name="google/gemma-2-2b",
-    layer=12,
+    model_name="meta-llama/Llama-3.3-70B-Instruct",
+    layer=40,
     hook_point="layer_output",
     device_map="auto",
     torch_dtype=torch.bfloat16,
@@ -243,12 +243,12 @@ steered_model = SteeredGemma2(
 For separate classification and steering layers, keep the same `Steerer` and pass explicit classify/steer hook locations:
 
 ```python
-steered_model = SteeredGemma2(
+steered_model = SteeredCausalLM(
     steerer,
-    model_name="google/gemma-2-2b",
-    classify_layer=12,
+    model_name="meta-llama/Llama-3.3-70B-Instruct",
+    classify_layer=40,
     classify_hook_point="layer_output",
-    steer_layer=20,
+    steer_layer=60,
     steer_hook_point="layer_output",
     device_map="auto",
     torch_dtype=torch.bfloat16,
@@ -259,21 +259,21 @@ The classification hook must run before the steering hook. In practice this mean
 
 ---
 
-## How `SteeredGemma2` uses a `Steerer`
+## How `SteeredCausalLM` uses a `Steerer`
 
-`SteeredGemma2` wraps a Gemma 2 causal language model and registers either one forward hook or two forward hooks. With the backward-compatible `layer` and `hook_point` arguments, classification and steering happen in the same hook. With the explicit `classify_*` and `steer_*` arguments, classification can happen at an earlier layer and steering can happen later.
+`SteeredCausalLM` wraps a decoder-only Hugging Face causal language model and registers either one forward hook or two forward hooks. With the backward-compatible `layer` and `hook_point` arguments, classification and steering happen in the same hook. With the explicit `classify_*` and `steer_*` arguments, classification can happen at an earlier layer and steering can happen later.
 
 The backward-compatible core pattern is:
 
 ```python
-from gemma2_steering import SteeredGemma2, ZeroSteerer
+from llm_steering import SteeredCausalLM, ZeroSteerer
 
 steerer = ZeroSteerer()
 
-steered_model = SteeredGemma2(
+steered_model = SteeredCausalLM(
     steerer,
-    model_name="google/gemma-2-2b",
-    layer=12,
+    model_name="meta-llama/Llama-3.3-70B-Instruct",
+    layer=40,
     hook_point="layer_output",
     device_map="auto",
 )
@@ -282,12 +282,12 @@ steered_model = SteeredGemma2(
 The separate classify/steer pattern is:
 
 ```python
-steered_model = SteeredGemma2(
+steered_model = SteeredCausalLM(
     steerer,
-    model_name="google/gemma-2-2b",
-    classify_layer=12,
+    model_name="meta-llama/Llama-3.3-70B-Instruct",
+    classify_layer=40,
     classify_hook_point="layer_output",
-    steer_layer=20,
+    steer_layer=60,
     steer_hook_point="layer_output",
     device_map="auto",
 )
@@ -295,7 +295,7 @@ steered_model = SteeredGemma2(
 
 During each forward pass:
 
-1. Gemma 2 computes the classification hooked module.
+1. The base model computes the classification hooked module.
 2. The classify hook receives the module output.
 3. The wrapper extracts the last-token hidden state:
 
@@ -326,7 +326,7 @@ This means steering is applied during normal `.forward(...)` calls and during `.
 
 ## Hook points
 
-`SteeredGemma2` supports three hook points.
+`SteeredCausalLM` supports three hook points.
 
 ### `layer_output`
 
@@ -355,16 +355,16 @@ This attaches the hook after the layer’s MLP module.
 You can also move the hooks after initialization. The old aliases move both hooks together:
 
 ```python
-steered_model.attach_hook(layer=18, hook_point="layer_output")
+steered_model.attach_hook(layer=40, hook_point="layer_output")
 ```
 
 Or you can move them separately:
 
 ```python
 steered_model.attach_hook(
-    classify_layer=12,
+    classify_layer=40,
     classify_hook_point="layer_output",
-    steer_layer=20,
+    steer_layer=60,
     steer_hook_point="layer_output",
 )
 ```
@@ -406,30 +406,30 @@ This is useful for comparing steered and unsteered behavior using the same in-me
 
 ## Notebook overview
 
-The notebook in `notebooks/gemma2_benchmarks.ipynb` is intended to be run in Google Colab.
+The notebook in `notebooks/gemma2_benchmarks.ipynb` is intended to be run on an H100-backed environment.
 
 At a high level, it does the following:
 
 1. Clones this repo from GitHub.
 2. Installs the package.
 3. Logs into Hugging Face.
-4. Loads `google/gemma-2-2b`.
+4. Loads `meta-llama/Llama-3.3-70B-Instruct`.
 5. Creates one or more `Steerer` objects.
-6. Wraps the model with `SteeredGemma2`.
+6. Wraps the model with `SteeredCausalLM`.
 7. Runs one benchmark per notebook cell.
 
 The basic model setup looks like this:
 
 ```python
 import torch
-from gemma2_steering import SteeredGemma2, ZeroSteerer
+from llm_steering import SteeredCausalLM, ZeroSteerer
 
 steerer = ZeroSteerer()
 
-model = SteeredGemma2(
+model = SteeredCausalLM(
     steerer,
-    model_name="google/gemma-2-2b",
-    layer=12,
+    model_name="meta-llama/Llama-3.3-70B-Instruct",
+    layer=40,
     hook_point="layer_output",
     device_map="auto",
     torch_dtype=torch.bfloat16,
@@ -442,7 +442,7 @@ Once the wrapper is created, the same model object can be used for generation an
 
 ## Benchmarking steered LLMs
 
-The repo is designed so that the same `SteeredGemma2` object can be evaluated with `lm-evaluation-harness`.
+The repo is designed so that the same `SteeredCausalLM` object can be evaluated with `lm-evaluation-harness`.
 
 The notebook uses one cell per benchmark. The included benchmark set is meant to cover several different behavioral axes:
 
@@ -460,12 +460,12 @@ benchmarks/sycophancy_on_nlp_survey.yaml
 
 It is kept in the repo because sycophancy task availability and naming can vary across `lm-evaluation-harness` versions. Keeping the YAML local gives the repo a stable task definition that the notebook can point to directly.
 
-The adapter in `gemma2_steering/lm_eval.py` lets the benchmark code evaluate the wrapped model rather than a fresh model loaded separately by the harness. This is important because the benchmark should measure the steered model, not an unmodified Gemma instance.
+The adapter in `llm_steering/lm_eval.py` lets the benchmark code evaluate the wrapped model rather than a fresh model loaded separately by the harness. This is important because the benchmark should measure the steered model, not an unmodified model instance.
 
 The notebook follows this general pattern:
 
 ```python
-from gemma2_steering import simple_evaluate
+from llm_steering import simple_evaluate
 
 results = simple_evaluate(
     model=steered_model,
@@ -503,7 +503,7 @@ Run tests with:
 pytest -q
 ```
 
-The tests use a tiny fake decoder model, not Gemma 2. They are designed to run quickly on CPU and do not require a Hugging Face token.
+The tests use a tiny fake decoder model, not real model weights. They are designed to run quickly on CPU and do not require a Hugging Face token.
 
 The tests check the main steering behaviors:
 
@@ -514,4 +514,4 @@ The tests check the main steering behaviors:
 * disabling classification and steering works;
 * classification and steering can use separate hooks;
 * invalid classify-after-steer hook configurations are rejected;
-* the hooks can be attached without loading Gemma 2.
+* the hooks can be attached without loading the full Llama model.
diff --git a/gemma2_steering/__init__.py b/llm_steering/__init__.py
similarity index 80%
rename from gemma2_steering/__init__.py
rename to llm_steering/__init__.py
index 445f245..9111a33 100644
--- a/gemma2_steering/__init__.py
+++ b/llm_steering/__init__.py
@@ -1,9 +1,9 @@
 from .lm_eval import as_lm_eval_model, simple_evaluate
-from .model import SteeredGemma2
+from .model import SteeredCausalLM
 from .steerers import ConstantSteerer, Steerer, ZeroSteerer
 
 __all__ = [
-    "SteeredGemma2",
+    "SteeredCausalLM",
     "Steerer",
     "ZeroSteerer",
     "ConstantSteerer",
diff --git a/gemma2_steering/lm_eval.py b/llm_steering/lm_eval.py
similarity index 83%
rename from gemma2_steering/lm_eval.py
rename to llm_steering/lm_eval.py
index 0d86d89..d1f68cd 100644
--- a/gemma2_steering/lm_eval.py
+++ b/llm_steering/lm_eval.py
@@ -3,14 +3,14 @@ from __future__ import annotations
 from pathlib import Path
 from typing import Any, Sequence
 
-from .model import SteeredGemma2
+from .model import SteeredCausalLM
 
 
-def as_lm_eval_model(steered: SteeredGemma2, **hf_lm_kwargs: Any) -> Any:
+def as_lm_eval_model(steered: SteeredCausalLM, **hf_lm_kwargs: Any) -> Any:
     """Wrap the hooked HF model for lm-evaluation-harness.
 
     The harness sees the underlying Transformers model, but the hook remains
-    installed because SteeredGemma2 registered it on that model in-place.
+    installed because SteeredCausalLM registered it on that model in-place.
     """
     from lm_eval.models.huggingface import HFLM
 
@@ -23,7 +23,7 @@ def as_lm_eval_model(steered: SteeredGemma2, **hf_lm_kwargs: Any) -> Any:
 
 
 def simple_evaluate(
-    steered: SteeredGemma2,
+    steered: SteeredCausalLM,
     tasks: str | Sequence[str],
     *,
     include_path: str | Path | None = None,
diff --git a/gemma2_steering/model.py b/llm_steering/model.py
similarity index 98%
rename from gemma2_steering/model.py
rename to llm_steering/model.py
index 52ef19d..0767067 100644
--- a/gemma2_steering/model.py
+++ b/llm_steering/model.py
@@ -9,8 +9,8 @@ from torch import nn
 from .steerers import Steerer
 
 
-class SteeredGemma2(nn.Module):
-    """Gemma-2 wrapper with separate classify and steer hooks.
+class SteeredCausalLM(nn.Module):
+    """Decoder-only causal LM wrapper with separate classify and steer hooks.
 
     By default classification and steering happen at the same decoder-layer
     hook, preserving the original behavior: the steerer sees the last token's
@@ -34,7 +34,7 @@ class SteeredGemma2(nn.Module):
         *,
         model: nn.Module | None = None,
         tokenizer: Any | None = None,
-        model_name: str = "google/gemma-2-2b",
+        model_name: str = "meta-llama/Llama-3.3-70B-Instruct",
         layer: int = 0,
         hook_point: str = "layer_output",
         classify_layer: int | None = None,
diff --git a/gemma2_steering/steerers.py b/llm_steering/steerers.py
similarity index 100%
rename from gemma2_steering/steerers.py
rename to llm_steering/steerers.py
diff --git a/notebooks/gemma2_benchmarks.ipynb b/notebooks/gemma2_benchmarks.ipynb
index 6e18695..1b31f85 100644
--- a/notebooks/gemma2_benchmarks.ipynb
+++ b/notebooks/gemma2_benchmarks.ipynb
@@ -1,466 +1,444 @@
 {
-  "cells": [
-    {
-      "cell_type": "markdown",
-      "id": "7d6a7a33",
-      "metadata": {
-        "id": "7d6a7a33"
-      },
-      "source": [
-        "# Gemma 2 steering benchmarks\n",
-        "\n",
-        "This notebook clones the repo from GitHub, loads `google/gemma-2-2b`, attaches the steering hook, and runs one `lm-evaluation-harness` cell per benchmark.\n",
-        "\n",
-        "Defaults use `ZeroSteerer` so you can compare against a no-op steered wrapper before swapping in a real steering method.\n",
-        "\n",
-        "In the future, we would like to implement a live steerer using CAM-compatible algorithms, which is simple to test using this notebook."
-      ]
-    },
-    {
-      "cell_type": "code",
-      "execution_count": 1,
-      "id": "9dfcc27a",
-      "metadata": {
-        "colab": {
-          "base_uri": "https://localhost:8080/"
-        },
-        "id": "9dfcc27a",
-        "outputId": "3d364647-b119-4fb3-b672-8a47328df09a"
-      },
-      "outputs": [
-        {
-          "output_type": "stream",
-          "name": "stdout",
-          "text": [
-            "/content/cam_enabled_steering_llm\n",
-            "  Installing build dependencies ... \u001b[?25l\u001b[?25hdone\n",
-            "  Checking if build backend supports build_editable ... \u001b[?25l\u001b[?25hdone\n",
-            "  Getting requirements to build editable ... \u001b[?25l\u001b[?25hdone\n",
-            "  Preparing editable metadata (pyproject.toml) ... \u001b[?25l\u001b[?25hdone\n",
-            "\u001b[2K     \u001b[90m━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━\u001b[0m \u001b[32m58.9/58.9 kB\u001b[0m \u001b[31m6.9 MB/s\u001b[0m eta \u001b[36m0:00:00\u001b[0m\n",
-            "\u001b[?25h  Preparing metadata (setup.py) ... \u001b[?25l\u001b[?25hdone\n",
-            "  Preparing metadata (setup.py) ... \u001b[?25l\u001b[?25hdone\n",
-            "  Preparing metadata (setup.py) ... \u001b[?25l\u001b[?25hdone\n",
-            "\u001b[2K   \u001b[90m━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━\u001b[0m \u001b[32m8.9/8.9 MB\u001b[0m \u001b[31m154.1 MB/s\u001b[0m eta \u001b[36m0:00:00\u001b[0m\n",
-            "\u001b[2K   \u001b[90m━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━\u001b[0m \u001b[32m84.1/84.1 kB\u001b[0m \u001b[31m10.3 MB/s\u001b[0m eta \u001b[36m0:00:00\u001b[0m\n",
-            "\u001b[2K   \u001b[90m━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━\u001b[0m \u001b[32m100.8/100.8 kB\u001b[0m \u001b[31m12.5 MB/s\u001b[0m eta \u001b[36m0:00:00\u001b[0m\n",
-            "\u001b[2K   \u001b[90m━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━\u001b[0m \u001b[32m91.1/91.1 kB\u001b[0m \u001b[31m12.1 MB/s\u001b[0m eta \u001b[36m0:00:00\u001b[0m\n",
-            "\u001b[?25h  Building editable for gemma2-steering-research (pyproject.toml) ... \u001b[?25l\u001b[?25hdone\n",
-            "  Building wheel for rouge-score (setup.py) ... \u001b[?25l\u001b[?25hdone\n",
-            "  Building wheel for sqlitedict (setup.py) ... \u001b[?25l\u001b[?25hdone\n",
-            "  Building wheel for word2number (setup.py) ... \u001b[?25l\u001b[?25hdone\n"
-          ]
-        }
-      ],
-      "source": [
-        "#@title Clone Repo\n",
-        "REPO_URL = \"https://github.com/JDub323/cam_enabled_steering_llm.git\"\n",
-        "REPO_DIR = \"cam_enabled_steering_llm\"\n",
-        "\n",
-        "import os\n",
-        "if not os.path.exists(REPO_DIR):\n",
-        "    !git clone -q $REPO_URL\n",
-        "%cd $REPO_DIR\n",
-        "!pip install -q -e \".[eval]\""
-      ]
-    },
-    {
-      "cell_type": "code",
-      "execution_count": 2,
-      "id": "53405981",
-      "metadata": {
-        "id": "53405981"
-      },
-      "outputs": [],
-      "source": [
-        "#@title Authenticate for gated Gemma weights\n",
-        "from huggingface_hub import login\n",
-        "login()"
-      ]
-    },
-    {
-      "cell_type": "code",
-      "execution_count": null,
-      "id": "00e69f57",
-      "metadata": {
-        "id": "00e69f57"
-      },
-      "outputs": [],
-      "source": [
-        "#@title Download Gemma, get Steered Model\n",
-        "# this takes about\n",
-        "import torch\n",
-        "import lm_eval\n",
-        "from lm_eval.tasks import TaskManager\n",
-        "\n",
-        "from gemma2_steering import SteeredGemma2, ZeroSteerer, ConstantSteerer, as_lm_eval_model\n",
-        "\n",
-        "MODEL_NAME = \"google/gemma-2-9b\" # this increases\n",
-        "LAYER = 12\n",
-        "HOOK_POINT = \"layer_output\"  # options: layer_output, post_attention, post_mlp\n",
-        "\n",
-        "model = SteeredGemma2(\n",
-        "    ZeroSteerer(),\n",
-        "    model_name=MODEL_NAME,\n",
-        "    layer=LAYER,\n",
-        "    hook_point=HOOK_POINT,\n",
-        "    device_map=\"auto\",\n",
-        "    torch_dtype=torch.bfloat16,\n",
-        ")\n"
-      ]
-    },
-    {
-      "cell_type": "code",
-      "source": [
-        "#@title Benchmark Configs\n",
-        "SAMPLE_LIMIT=50 # set to None to run full benchmarks\n",
-        "NUM_FEWSHOT=0"
-      ],
-      "metadata": {
-        "id": "PvCqq_jYp5yy"
-      },
-      "id": "PvCqq_jYp5yy",
-      "execution_count": null,
-      "outputs": []
-    },
-    {
-      "cell_type": "code",
-      "source": [
-        "# @title Make a model harness\n",
-        "# batch size 3 was a bit too bit for google colab on an L4 GPU, but that is what is selected\n",
-        "# automatically. Use batch_size 2 instead\n",
-        "lm = as_lm_eval_model(model, tokenizer=model.tokenizer, batch_size='auto')\n",
-        "task_manager = TaskManager(include_path=\"benchmarks\")"
-      ],
-      "metadata": {
-        "id": "HavyqRqrvBw_"
-      },
-      "id": "HavyqRqrvBw_",
-      "execution_count": null,
-      "outputs": []
-    },
-    {
-      "cell_type": "code",
-      "execution_count": null,
-      "id": "25789639",
-      "metadata": {
-        "id": "25789639"
-      },
-      "outputs": [],
-      "source": [
-        "#@title Helper Functions\n",
-        "import gc\n",
-        "import torch\n",
-        "\n",
-        "def run_eval(task, *, limit=20, num_fewshot=0):\n",
-        "    \"\"\"Run one lm-eval task. Keep limit small on Colab; set limit=None for full evals.\"\"\"\n",
-        "    # Clear memory before running the evaluation to prevent fragmented OOMs\n",
-        "    gc.collect()\n",
-        "    torch.cuda.empty_cache()\n",
-        "\n",
-        "    result = lm_eval.simple_evaluate(\n",
-        "        model=lm,\n",
-        "        tasks=[task],\n",
-        "        task_manager=task_manager,\n",
-        "        limit=limit,\n",
-        "        num_fewshot=num_fewshot,\n",
-        "        log_samples=False,\n",
-        "    )\n",
-        "\n",
-        "    # Clear memory after the evaluation\n",
-        "    gc.collect()\n",
-        "    torch.cuda.empty_cache()\n",
-        "\n",
-        "    return result\n",
-        "\n",
-        "\n",
-        "def try_first_available(tasks, *, limit=20, num_fewshot=0):\n",
-        "    errors = {}\n",
-        "    for task in tasks:\n",
-        "        try:\n",
-        "            print(f\"Trying {task!r}...\")\n",
-        "            return run_eval(task, limit=limit, num_fewshot=num_fewshot)\n",
-        "        except Exception as exc:\n",
-        "            errors[task] = repr(exc)\n",
-        "    print(\"None of the candidate task names worked. Inspect installed tasks with:\")\n",
-        "    print(\"!lm-eval ls tasks | grep -Ei 'hle|humanity|last_exam'\")\n",
-        "    raise RuntimeError(errors)\n"
-      ]
-    },
-    {
-      "cell_type": "code",
-      "execution_count": null,
-      "id": "f832b832",
-      "metadata": {
-        "id": "f832b832"
-      },
-      "outputs": [],
-      "source": [
-        "#@title Generation sanity check.\n",
-        "prompt = \"The capital of France is\"\n",
-        "inputs = model.tokenizer(prompt, return_tensors=\"pt\").to(model.device)\n",
-        "output = model.generate(**inputs, max_new_tokens=16)\n",
-        "print(model.tokenizer.decode(output[0], skip_special_tokens=True))\n",
-        "\n",
-        "# Disable steering without reloading Gemma:\n",
-        "with model.without_steering():\n",
-        "    output = model.generate(**inputs, max_new_tokens=16)\n",
-        "print(model.tokenizer.decode(output[0], skip_special_tokens=True))"
-      ]
-    },
-    {
-      "cell_type": "markdown",
-      "id": "b85483f7",
-      "metadata": {
-        "id": "b85483f7"
-      },
-      "source": [
-        "## Benchmark: MMLU"
-      ]
-    },
-    {
-      "cell_type": "code",
-      "execution_count": null,
-      "id": "70d91ff2",
-      "metadata": {
-        "collapsed": true,
-        "id": "70d91ff2"
-      },
-      "outputs": [],
-      "source": [
-        "# MMLU is large; use a small limit for Colab smoke tests.\n",
-        "mmlu_results = run_eval(\"mmlu\", limit=SAMPLE_LIMIT, num_fewshot=NUM_FEWSHOT)\n",
-        "mmlu_results[\"results\"]"
-      ]
-    },
-    {
-      "cell_type": "markdown",
-      "id": "4141ae93",
-      "metadata": {
-        "id": "4141ae93"
-      },
-      "source": [
-        "## Benchmark: Humanity's Last Exam (DOES NOT EXIST YET)"
-      ]
-    },
-    {
-      "cell_type": "code",
-      "execution_count": null,
-      "id": "5b699d7e",
-      "metadata": {
-        "collapsed": true,
-        "id": "5b699d7e"
-      },
-      "outputs": [],
-      "source": [
-        "# HLE availability depends on the installed lm-eval release.\n",
-        "# This tries common task names and prints an inspection command if unavailable.\n",
-        "# hle_results = try_first_available(\n",
-        "#     [\"humanitys_last_exam\", \"humanity_last_exam\", \"hle\", \"hle_text\"],\n",
-        "#     limit=10,\n",
-        "#     num_fewshot=0,\n",
-        "# )\n",
-        "# hle_results[\"results\"]"
-      ]
-    },
-    {
-      "cell_type": "markdown",
-      "id": "d9b0b9a7",
-      "metadata": {
-        "id": "d9b0b9a7"
-      },
-      "source": [
-        "## Benchmark: TruthfulQA"
-      ]
-    },
-    {
-      "cell_type": "code",
-      "execution_count": null,
-      "id": "0a998baf",
-      "metadata": {
-        "id": "0a998baf"
-      },
-      "outputs": [],
-      "source": [
-        "truthfulqa_results = run_eval(\"truthfulqa_mc2\", limit=SAMPLE_LIMIT, num_fewshot=NUM_FEWSHOT)\n",
-        "truthfulqa_results[\"results\"]"
-      ]
-    },
-    {
-      "cell_type": "markdown",
-      "id": "6af70761",
-      "metadata": {
-        "id": "6af70761"
-      },
-      "source": [
-        "## Benchmark: Sycophancy (IS BROKEN)"
-      ]
-    },
-    {
-      "cell_type": "code",
-      "execution_count": null,
-      "id": "1aac8c48",
-      "metadata": {
-        "collapsed": true,
-        "id": "1aac8c48"
-      },
-      "outputs": [],
-      "source": [
-        "# Local YAML in benchmarks/sycophancy_on_nlp_survey.yaml.\n",
-        "# sycophancy_results = run_eval(\"sycophancy_on_nlp_survey\", limit=50, num_fewshot=0)\n",
-        "# sycophancy_results[\"results\"]"
-      ]
-    },
-    {
-      "cell_type": "markdown",
-      "id": "499895e7",
-      "metadata": {
-        "id": "499895e7"
-      },
-      "source": [
-        "## Benchmark: WMDP dangerous-knowledge / malicious-capability probe"
-      ]
-    },
-    {
-      "cell_type": "code",
-      "execution_count": null,
-      "id": "555355a1",
-      "metadata": {
-        "collapsed": true,
-        "id": "555355a1"
-      },
-      "outputs": [],
-      "source": [
-        "# WMDP covers biosecurity, cybersecurity, and chemical-security multiple-choice questions.\n",
-        "wmdp_results = run_eval(\"wmdp\", limit=SAMPLE_LIMIT, num_fewshot=NUM_FEWSHOT)\n",
-        "wmdp_results[\"results\"]"
-      ]
-    },
-    {
-      "cell_type": "markdown",
-      "id": "3362150b",
-      "metadata": {
-        "id": "3362150b"
-      },
-      "source": [
-        "## Swap in a constant vector\n",
-        "\n",
-        "`ConstantSteerer` requires a batched vector. This is intentionally explicit; use `[1, hidden_size]` for a vector shared across the batch."
-      ]
-    },
-    {
-      "cell_type": "code",
-      "execution_count": null,
-      "id": "d0cc821f",
-      "metadata": {
-        "id": "d0cc821f"
-      },
-      "outputs": [],
-      "source": [
-        "hidden = model.config.hidden_size\n",
-        "constant = torch.zeros(1, hidden, device=model.device, dtype=torch.bfloat16)\n",
-        "constant[:, 0] = 1.0\n",
-        "\n",
-        "model.steerer = ConstantSteerer(constant)\n",
-        "# Re-run any benchmark cell above to compare against ZeroSteerer."
-      ]
-    },
-    {
-      "cell_type": "markdown",
-      "metadata": {
-        "id": "ce22e1c1"
-      },
-      "source": [
-        "## View Results\n",
-        "Print the results using `lm_eval`'s built-in table formatter."
-      ],
-      "id": "ce22e1c1"
-    },
-    {
-      "cell_type": "code",
-      "metadata": {
-        "id": "ce22a9f6"
-      },
-      "source": [
-        "from lm_eval.utils import make_table\n",
-        "\n",
-        "print(\"=== MMLU Results ===\")\n",
-        "if 'mmlu_results' in globals():\n",
-        "    print(make_table(mmlu_results))\n",
-        "\n",
-        "print(\"\\n=== TruthfulQA Results ===\")\n",
-        "if 'truthfulqa_results' in globals():\n",
-        "    print(make_table(truthfulqa_results))\n",
-        "\n",
-        "print(\"\\n=== WMDP Results ===\")\n",
-        "if 'wmdp_results' in globals():\n",
-        "    print(make_table(wmdp_results))"
-      ],
-      "id": "ce22a9f6",
-      "execution_count": null,
-      "outputs": []
-    },
-    {
-      "cell_type": "code",
-      "metadata": {
-        "id": "858d7f3f"
-      },
-      "source": [
-        "def print_summary_accuracy(results_dict, suite_name):\n",
-        "    if not results_dict or 'results' not in results_dict:\n",
-        "        return\n",
-        "\n",
-        "    results = results_dict['results']\n",
-        "    acc_values = []\n",
-        "\n",
-        "    # Try to find the exact suite name first (aggregate score)\n",
-        "    for task_name, metrics in results.items():\n",
-        "        # lm_eval uses various keys for accuracy depending on the task\n",
-        "        acc = metrics.get('acc,none') or metrics.get('acc_norm,none') or metrics.get('mc2') or metrics.get('acc')\n",
-        "\n",
-        "        if acc is not None:\n",
-        "            # If it's an aggregate key (e.g., 'mmlu' or 'wmdp'), print it directly\n",
-        "            if task_name == suite_name.lower():\n",
-        "                print(f\"{suite_name} Overall Accuracy: {acc * 100:.2f}%\")\n",
-        "                return\n",
-        "            acc_values.append(acc)\n",
-        "\n",
-        "    # If no single aggregate key is found, average the subtasks\n",
-        "    if acc_values:\n",
-        "        overall_acc = sum(acc_values) / len(acc_values)\n",
-        "        print(f\"{suite_name} Overall Accuracy (averaged): {overall_acc * 100:.2f}%\")\n",
-        "    else:\n",
-        "        print(f\"{suite_name}: No standard accuracy metric found.\")\n",
-        "\n",
-        "print(\"=== Benchmark Accuracy Summaries ===\")\n",
-        "if 'mmlu_results' in globals():\n",
-        "    print_summary_accuracy(mmlu_results, \"MMLU\")\n",
-        "if 'truthfulqa_results' in globals():\n",
-        "    # TruthfulQA usually reports 'mc2' or 'acc'\n",
-        "    print_summary_accuracy(truthfulqa_results, \"TruthfulQA\")\n",
-        "if 'wmdp_results' in globals():\n",
-        "    print_summary_accuracy(wmdp_results, \"WMDP\")\n"
-      ],
-      "id": "858d7f3f",
-      "execution_count": null,
-      "outputs": []
-    }
-  ],
-  "metadata": {
-    "accelerator": "GPU",
+ "cells": [
+  {
+   "cell_type": "markdown",
+   "id": "7d6a7a33",
+   "metadata": {
+    "id": "7d6a7a33"
+   },
+   "source": [
+    "# Llama 3.3 70B steering benchmarks\n",
+    "\n",
+    "This notebook clones the repo from GitHub, loads `meta-llama/Llama-3.3-70B-Instruct`, attaches the steering hook, and runs one `lm-evaluation-harness` cell per benchmark.\n",
+    "\n",
+    "Defaults use `ZeroSteerer` so you can compare against a no-op steered wrapper before swapping in a real steering method.\n",
+    "\n",
+    "This notebook is configured for H100-backed runs: it loads the 70B model in `bfloat16` with `device_map=\"auto\"` rather than adding quantization for smaller GPUs.\n",
+    "\n",
+    "In the future, we would like to implement a live steerer using CAM-compatible algorithms, which is simple to test using this notebook.\n"
+   ]
+  },
+  {
+   "cell_type": "code",
+   "execution_count": null,
+   "id": "9dfcc27a",
+   "metadata": {
     "colab": {
-      "provenance": [],
-      "machine_shape": "hm",
-      "gpuType": "H100"
+     "base_uri": "https://localhost:8080/"
     },
-    "kernelspec": {
-      "display_name": "Python 3",
-      "name": "python3"
-    },
-    "language_info": {
-      "name": "python"
-    }
+    "id": "9dfcc27a",
+    "outputId": "3d364647-b119-4fb3-b672-8a47328df09a"
+   },
+   "outputs": [],
+   "source": [
+    "#@title Clone Repo\n",
+    "REPO_URL = \"https://github.com/JDub323/cam_enabled_steering_llm.git\"\n",
+    "REPO_DIR = \"cam_enabled_steering_llm\"\n",
+    "\n",
+    "import os\n",
+    "if not os.path.exists(REPO_DIR):\n",
+    "    !git clone -q $REPO_URL\n",
+    "%cd $REPO_DIR\n",
+    "!pip install -q -e \".[eval]\""
+   ]
+  },
+  {
+   "cell_type": "code",
+   "execution_count": null,
+   "id": "53405981",
+   "metadata": {
+    "id": "53405981"
+   },
+   "outputs": [],
+   "source": [
+    "#@title Authenticate for gated Llama weights\n",
+    "from huggingface_hub import login\n",
+    "login()\n"
+   ]
+  },
+  {
+   "cell_type": "code",
+   "execution_count": null,
+   "id": "00e69f57",
+   "metadata": {
+    "id": "00e69f57"
+   },
+   "outputs": [],
+   "source": [
+    "#@title Download Llama 3.3 70B Instruct, get steered model\n",
+    "# Intended for H100-backed runs. Llama 3.3 70B in bfloat16 should fit comfortably on an H100.\n",
+    "import torch\n",
+    "import lm_eval\n",
+    "from lm_eval.tasks import TaskManager\n",
+    "\n",
+    "from llm_steering import SteeredCausalLM, ZeroSteerer, ConstantSteerer, as_lm_eval_model\n",
+    "\n",
+    "MODEL_NAME = \"meta-llama/Llama-3.3-70B-Instruct\"\n",
+    "LAYER = 40  # mid-model layer for the 80-layer Llama 3.3 70B architecture\n",
+    "HOOK_POINT = \"layer_output\"  # options: layer_output, post_attention, post_mlp\n",
+    "\n",
+    "model = SteeredCausalLM(\n",
+    "    ZeroSteerer(),\n",
+    "    model_name=MODEL_NAME,\n",
+    "    layer=LAYER,\n",
+    "    hook_point=HOOK_POINT,\n",
+    "    device_map=\"auto\",\n",
+    "    torch_dtype=torch.bfloat16,\n",
+    ")\n"
+   ]
+  },
+  {
+   "cell_type": "code",
+   "source": [
+    "#@title Benchmark Configs\n",
+    "SAMPLE_LIMIT=50 # set to None to run full benchmarks\n",
+    "NUM_FEWSHOT=0"
+   ],
+   "metadata": {
+    "id": "PvCqq_jYp5yy"
+   },
+   "id": "PvCqq_jYp5yy",
+   "execution_count": null,
+   "outputs": []
+  },
+  {
+   "cell_type": "code",
+   "source": [
+    "# @title Make a model harness\n",
+    "# H100 runs should have enough memory for automatic lm-eval batching.\n",
+    "lm = as_lm_eval_model(model, tokenizer=model.tokenizer, batch_size='auto')\n",
+    "task_manager = TaskManager(include_path=\"benchmarks\")\n"
+   ],
+   "metadata": {
+    "id": "HavyqRqrvBw_"
+   },
+   "id": "HavyqRqrvBw_",
+   "execution_count": null,
+   "outputs": []
+  },
+  {
+   "cell_type": "code",
+   "execution_count": null,
+   "id": "25789639",
+   "metadata": {
+    "id": "25789639"
+   },
+   "outputs": [],
+   "source": [
+    "#@title Helper Functions\n",
+    "import gc\n",
+    "import torch\n",
+    "\n",
+    "def run_eval(task, *, limit=20, num_fewshot=0):\n",
+    "    \"\"\"Run one lm-eval task. Set limit=None for full H100-backed evals.\"\"\"\n",
+    "    # Clear memory before running the evaluation to prevent fragmented OOMs\n",
+    "    gc.collect()\n",
+    "    torch.cuda.empty_cache()\n",
+    "\n",
+    "    result = lm_eval.simple_evaluate(\n",
+    "        model=lm,\n",
+    "        tasks=[task],\n",
+    "        task_manager=task_manager,\n",
+    "        limit=limit,\n",
+    "        num_fewshot=num_fewshot,\n",
+    "        log_samples=False,\n",
+    "    )\n",
+    "\n",
+    "    # Clear memory after the evaluation\n",
+    "    gc.collect()\n",
+    "    torch.cuda.empty_cache()\n",
+    "\n",
+    "    return result\n",
+    "\n",
+    "\n",
+    "def try_first_available(tasks, *, limit=20, num_fewshot=0):\n",
+    "    errors = {}\n",
+    "    for task in tasks:\n",
+    "        try:\n",
+    "            print(f\"Trying {task!r}...\")\n",
+    "            return run_eval(task, limit=limit, num_fewshot=num_fewshot)\n",
+    "        except Exception as exc:\n",
+    "            errors[task] = repr(exc)\n",
+    "    print(\"None of the candidate task names worked. Inspect installed tasks with:\")\n",
+    "    print(\"python -m lm_eval --tasks list\")\n",
+    "    print(errors)\n",
+    "    return None\n"
+   ]
+  },
+  {
+   "cell_type": "code",
+   "execution_count": null,
+   "id": "f832b832",
+   "metadata": {
+    "id": "f832b832"
+   },
+   "outputs": [],
+   "source": [
+    "#@title Generation sanity check.\n",
+    "prompt = \"The capital of France is\"\n",
+    "inputs = model.tokenizer(prompt, return_tensors=\"pt\").to(model.device)\n",
+    "output = model.generate(**inputs, max_new_tokens=16)\n",
+    "print(model.tokenizer.decode(output[0], skip_special_tokens=True))\n",
+    "\n",
+    "# Disable steering without reloading the model:\n",
+    "with model.without_steering():\n",
+    "    output = model.generate(**inputs, max_new_tokens=16)\n",
+    "print(model.tokenizer.decode(output[0], skip_special_tokens=True))\n"
+   ]
+  },
+  {
+   "cell_type": "markdown",
+   "id": "b85483f7",
+   "metadata": {
+    "id": "b85483f7"
+   },
+   "source": [
+    "## Benchmark: MMLU"
+   ]
+  },
+  {
+   "cell_type": "code",
+   "execution_count": null,
+   "id": "70d91ff2",
+   "metadata": {
+    "collapsed": true,
+    "id": "70d91ff2"
+   },
+   "outputs": [],
+   "source": [
+    "# MMLU is large; use SAMPLE_LIMIT for smoke tests, or set SAMPLE_LIMIT=None for full H100-backed evals.\n",
+    "mmlu_results = run_eval(\"mmlu\", limit=SAMPLE_LIMIT, num_fewshot=NUM_FEWSHOT)\n",
+    "mmlu_results[\"results\"]\n"
+   ]
+  },
+  {
+   "cell_type": "markdown",
+   "id": "4141ae93",
+   "metadata": {
+    "id": "4141ae93"
+   },
+   "source": [
+    "## Benchmark: Humanity's Last Exam (DOES NOT EXIST YET)"
+   ]
+  },
+  {
+   "cell_type": "code",
+   "execution_count": null,
+   "id": "5b699d7e",
+   "metadata": {
+    "collapsed": true,
+    "id": "5b699d7e"
+   },
+   "outputs": [],
+   "source": [
+    "# HLE availability depends on the installed lm-eval release.\n",
+    "# This tries common task names and prints an inspection command if unavailable.\n",
+    "# hle_results = try_first_available(\n",
+    "#     [\"humanitys_last_exam\", \"humanity_last_exam\", \"hle\", \"hle_text\"],\n",
+    "#     limit=10,\n",
+    "#     num_fewshot=0,\n",
+    "# )\n",
+    "# hle_results[\"results\"]"
+   ]
+  },
+  {
+   "cell_type": "markdown",
+   "id": "d9b0b9a7",
+   "metadata": {
+    "id": "d9b0b9a7"
+   },
+   "source": [
+    "## Benchmark: TruthfulQA"
+   ]
+  },
+  {
+   "cell_type": "code",
+   "execution_count": null,
+   "id": "0a998baf",
+   "metadata": {
+    "id": "0a998baf"
+   },
+   "outputs": [],
+   "source": [
+    "truthfulqa_results = run_eval(\"truthfulqa_mc2\", limit=SAMPLE_LIMIT, num_fewshot=NUM_FEWSHOT)\n",
+    "truthfulqa_results[\"results\"]"
+   ]
+  },
+  {
+   "cell_type": "markdown",
+   "id": "6af70761",
+   "metadata": {
+    "id": "6af70761"
+   },
+   "source": [
+    "## Benchmark: Sycophancy (IS BROKEN)"
+   ]
+  },
+  {
+   "cell_type": "code",
+   "execution_count": null,
+   "id": "1aac8c48",
+   "metadata": {
+    "collapsed": true,
+    "id": "1aac8c48"
+   },
+   "outputs": [],
+   "source": [
+    "# Local YAML in benchmarks/sycophancy_on_nlp_survey.yaml.\n",
+    "# sycophancy_results = run_eval(\"sycophancy_on_nlp_survey\", limit=50, num_fewshot=0)\n",
+    "# sycophancy_results[\"results\"]"
+   ]
+  },
+  {
+   "cell_type": "markdown",
+   "id": "499895e7",
+   "metadata": {
+    "id": "499895e7"
+   },
+   "source": [
+    "## Benchmark: WMDP dangerous-knowledge / malicious-capability probe"
+   ]
+  },
+  {
+   "cell_type": "code",
+   "execution_count": null,
+   "id": "555355a1",
+   "metadata": {
+    "collapsed": true,
+    "id": "555355a1"
+   },
+   "outputs": [],
+   "source": [
+    "# WMDP covers biosecurity, cybersecurity, and chemical-security multiple-choice questions.\n",
+    "wmdp_results = run_eval(\"wmdp\", limit=SAMPLE_LIMIT, num_fewshot=NUM_FEWSHOT)\n",
+    "wmdp_results[\"results\"]"
+   ]
+  },
+  {
+   "cell_type": "markdown",
+   "id": "3362150b",
+   "metadata": {
+    "id": "3362150b"
+   },
+   "source": [
+    "## Swap in a constant vector\n",
+    "\n",
+    "`ConstantSteerer` requires a batched vector. This is intentionally explicit; use `[1, hidden_size]` for a vector shared across the batch."
+   ]
+  },
+  {
+   "cell_type": "code",
+   "execution_count": null,
+   "id": "d0cc821f",
+   "metadata": {
+    "id": "d0cc821f"
+   },
+   "outputs": [],
+   "source": [
+    "hidden = model.config.hidden_size\n",
+    "constant = torch.zeros(1, hidden, device=model.device, dtype=torch.bfloat16)\n",
+    "constant[:, 0] = 1.0\n",
+    "\n",
+    "model.steerer = ConstantSteerer(constant)\n",
+    "# Re-run any benchmark cell above to compare against ZeroSteerer."
+   ]
+  },
+  {
+   "cell_type": "markdown",
+   "metadata": {
+    "id": "ce22e1c1"
+   },
+   "source": [
+    "## View Results\n",
+    "Print the results using `lm_eval`'s built-in table formatter."
+   ],
+   "id": "ce22e1c1"
+  },
+  {
+   "cell_type": "code",
+   "metadata": {
+    "id": "ce22a9f6"
+   },
+   "source": [
+    "from lm_eval.utils import make_table\n",
+    "\n",
+    "print(\"=== MMLU Results ===\")\n",
+    "if 'mmlu_results' in globals():\n",
+    "    print(make_table(mmlu_results))\n",
+    "\n",
+    "print(\"\\n=== TruthfulQA Results ===\")\n",
+    "if 'truthfulqa_results' in globals():\n",
+    "    print(make_table(truthfulqa_results))\n",
+    "\n",
+    "print(\"\\n=== WMDP Results ===\")\n",
+    "if 'wmdp_results' in globals():\n",
+    "    print(make_table(wmdp_results))"
+   ],
+   "id": "ce22a9f6",
+   "execution_count": null,
+   "outputs": []
+  },
+  {
+   "cell_type": "code",
+   "metadata": {
+    "id": "858d7f3f"
+   },
+   "source": [
+    "def print_summary_accuracy(results_dict, suite_name):\n",
+    "    if not results_dict or 'results' not in results_dict:\n",
+    "        return\n",
+    "\n",
+    "    results = results_dict['results']\n",
+    "    acc_values = []\n",
+    "\n",
+    "    # Try to find the exact suite name first (aggregate score)\n",
+    "    for task_name, metrics in results.items():\n",
+    "        # lm_eval uses various keys for accuracy depending on the task\n",
+    "        acc = metrics.get('acc,none') or metrics.get('acc_norm,none') or metrics.get('mc2') or metrics.get('acc')\n",
+    "\n",
+    "        if acc is not None:\n",
+    "            # If it's an aggregate key (e.g., 'mmlu' or 'wmdp'), print it directly\n",
+    "            if task_name == suite_name.lower():\n",
+    "                print(f\"{suite_name} Overall Accuracy: {acc * 100:.2f}%\")\n",
+    "                return\n",
+    "            acc_values.append(acc)\n",
+    "\n",
+    "    # If no single aggregate key is found, average the subtasks\n",
+    "    if acc_values:\n",
+    "        overall_acc = sum(acc_values) / len(acc_values)\n",
+    "        print(f\"{suite_name} Overall Accuracy (averaged): {overall_acc * 100:.2f}%\")\n",
+    "    else:\n",
+    "        print(f\"{suite_name}: No standard accuracy metric found.\")\n",
+    "\n",
+    "print(\"=== Benchmark Accuracy Summaries ===\")\n",
+    "if 'mmlu_results' in globals():\n",
+    "    print_summary_accuracy(mmlu_results, \"MMLU\")\n",
+    "if 'truthfulqa_results' in globals():\n",
+    "    # TruthfulQA usually reports 'mc2' or 'acc'\n",
+    "    print_summary_accuracy(truthfulqa_results, \"TruthfulQA\")\n",
+    "if 'wmdp_results' in globals():\n",
+    "    print_summary_accuracy(wmdp_results, \"WMDP\")\n"
+   ],
+   "id": "858d7f3f",
+   "execution_count": null,
+   "outputs": []
+  }
+ ],
+ "metadata": {
+  "accelerator": "GPU",
+  "colab": {
+   "provenance": [],
+   "machine_shape": "hm",
+   "gpuType": "H100"
+  },
+  "kernelspec": {
+   "display_name": "Python 3",
+   "name": "python3"
   },
-  "nbformat": 4,
-  "nbformat_minor": 5
-}
\ No newline at end of file
+  "language_info": {
+   "name": "python"
+  }
+ },
+ "nbformat": 4,
+ "nbformat_minor": 5
+}
diff --git a/pyproject.toml b/pyproject.toml
index 78a4b29..fac491e 100644
--- a/pyproject.toml
+++ b/pyproject.toml
@@ -3,9 +3,9 @@ requires = ["setuptools>=68"]
 build-backend = "setuptools.build_meta"
 
 [project]
-name = "gemma2-steering-research"
+name = "llm-steering-research"
 version = "0.1.0"
-description = "Minimal residual-stream steering utilities for Gemma 2 interpretability research."
+description = "Minimal residual-stream steering utilities for decoder-only LLM interpretability research."
 readme = "README.md"
 requires-python = ">=3.10"
 dependencies = [
@@ -19,4 +19,4 @@ eval = ["lm-eval[hf]>=0.4.0", "datasets>=2.20", "huggingface-hub>=0.24"]
 dev = ["pytest>=8.0"]
 
 [tool.setuptools.packages.find]
-include = ["gemma2_steering*"]
+include = ["llm_steering*"]
diff --git a/tests/test_steering.py b/tests/test_steering.py
index b53e9c0..df46a82 100644
--- a/tests/test_steering.py
+++ b/tests/test_steering.py
@@ -4,7 +4,7 @@ import pytest
 import torch
 from torch import nn
 
-from gemma2_steering import ConstantSteerer, SteeredGemma2, Steerer, ZeroSteerer
+from llm_steering import ConstantSteerer, SteeredCausalLM, Steerer, ZeroSteerer
 
 
 class ToyLayer(nn.Module):
@@ -52,7 +52,7 @@ class RecordingSteerer(Steerer):
 
 def test_zero_steerer_is_noop_after_layer_output():
     toy = ToyModel()
-    wrapped = SteeredGemma2(ZeroSteerer(), model=toy, layer=0)
+    wrapped = SteeredCausalLM(ZeroSteerer(), model=toy, layer=0)
     x = torch.zeros(2, 4, 3)
     out = wrapped(inputs_embeds=x).last_hidden_state
     assert torch.allclose(out, torch.ones_like(x))
@@ -60,7 +60,7 @@ def test_zero_steerer_is_noop_after_layer_output():
 
 def test_constant_steerer_changes_only_last_token():
     toy = ToyModel()
-    wrapped = SteeredGemma2(ConstantSteerer(torch.tensor([[10.0, 20.0, 30.0]])), model=toy)
+    wrapped = SteeredCausalLM(ConstantSteerer(torch.tensor([[10.0, 20.0, 30.0]])), model=toy)
     out = wrapped(inputs_embeds=torch.zeros(2, 4, 3)).last_hidden_state
     assert torch.allclose(out[:, :-1, :], torch.ones(2, 3, 3))
     assert torch.allclose(out[:, -1, :], torch.tensor([[11.0, 21.0, 31.0]]).expand(2, 3))
@@ -68,7 +68,7 @@ def test_constant_steerer_changes_only_last_token():
 
 def test_can_classify_and_steer_at_different_layers():
     steerer = RecordingSteerer()
-    wrapped = SteeredGemma2(
+    wrapped = SteeredCausalLM(
         steerer,
         model=ToyModel(num_layers=2),
         classify_layer=0,
@@ -82,7 +82,7 @@ def test_can_classify_and_steer_at_different_layers():
 
 
 def test_same_classify_and_steer_hook_uses_one_handle():
-    wrapped = SteeredGemma2(
+    wrapped = SteeredCausalLM(
         ConstantSteerer(torch.tensor([[10.0, 20.0, 30.0]])),
         model=ToyModel(),
         classify_layer=0,
@@ -97,7 +97,7 @@ def test_same_classify_and_steer_hook_uses_one_handle():
 
 def test_classification_can_be_enabled_without_steering():
     steerer = RecordingSteerer()
-    wrapped = SteeredGemma2(
+    wrapped = SteeredCausalLM(
         steerer,
         model=ToyModel(num_layers=2),
         classify_layer=0,
@@ -113,7 +113,7 @@ def test_classification_can_be_enabled_without_steering():
 
 
 def test_steering_without_classification_raises():
-    wrapped = SteeredGemma2(
+    wrapped = SteeredCausalLM(
         ConstantSteerer(torch.ones(1, 3)),
         model=ToyModel(num_layers=2),
         classify_layer=0,
@@ -127,7 +127,7 @@ def test_steering_without_classification_raises():
 
 def test_classify_layer_must_not_follow_steer_layer():
     with pytest.raises(ValueError, match="classify_layer"):
-        SteeredGemma2(
+        SteeredCausalLM(
             ZeroSteerer(),
             model=ToyModel(num_layers=2),
             classify_layer=1,
@@ -137,7 +137,7 @@ def test_classify_layer_must_not_follow_steer_layer():
 
 def test_classify_hook_point_must_not_follow_steer_hook_point_on_same_layer():
     with pytest.raises(ValueError, match="classify_hook_point"):
-        SteeredGemma2(
+        SteeredCausalLM(
             ZeroSteerer(),
             model=ToyModel(),
             classify_layer=0,
@@ -149,7 +149,7 @@ def test_classify_hook_point_must_not_follow_steer_hook_point_on_same_layer():
 
 def test_without_steering_temporarily_disables_only_steering_hook():
     steerer = RecordingSteerer()
-    wrapped = SteeredGemma2(steerer, model=ToyModel(), layer=0)
+    wrapped = SteeredCausalLM(steerer, model=ToyModel(), layer=0)
     x = torch.zeros(1, 2, 3)
     with wrapped.without_steering():
         assert torch.allclose(wrapped(inputs_embeds=x).last_hidden_state[:, -1, :], torch.tensor([[1.0, 1.0, 1.0]]))
@@ -160,14 +160,14 @@ def test_without_steering_temporarily_disables_only_steering_hook():
 
 
 def test_without_classification_errors_if_steering_remains_enabled():
-    wrapped = SteeredGemma2(ConstantSteerer(torch.ones(1, 3)), model=ToyModel(), layer=0)
+    wrapped = SteeredCausalLM(ConstantSteerer(torch.ones(1, 3)), model=ToyModel(), layer=0)
     with wrapped.without_classification():
         with pytest.raises(RuntimeError, match="before classification produced"):
             wrapped(inputs_embeds=torch.zeros(1, 2, 3))
 
 
 def test_set_enabled_remains_backward_compatible():
-    wrapped = SteeredGemma2(ConstantSteerer(torch.tensor([[2.0, 2.0, 2.0]])), model=ToyModel())
+    wrapped = SteeredCausalLM(ConstantSteerer(torch.tensor([[2.0, 2.0, 2.0]])), model=ToyModel())
     x = torch.zeros(1, 2, 3)
     assert torch.allclose(wrapped(inputs_embeds=x).last_hidden_state[:, -1, :], torch.tensor([[3.0, 3.0, 3.0]]))
     wrapped.set_enabled(False)
@@ -180,7 +180,7 @@ def test_set_enabled_remains_backward_compatible():
 
 def test_invalid_steerer_rejected():
     with pytest.raises(TypeError):
-        SteeredGemma2(object(), model=ToyModel())
+        SteeredCausalLM(object(), model=ToyModel())
 
 
 def test_unbatched_constant_vector_rejected():
@@ -189,13 +189,13 @@ def test_unbatched_constant_vector_rejected():
 
 
 def test_steerer_must_return_batched_shape():
-    wrapped = SteeredGemma2(BadShapeSteerer(), model=ToyModel())
+    wrapped = SteeredCausalLM(BadShapeSteerer(), model=ToyModel())
     with pytest.raises(ValueError, match="Steering vector must have shape"):
         wrapped(inputs_embeds=torch.zeros(2, 4, 3))
 
 
 def test_can_move_hooks_together_with_backward_compatible_aliases():
-    wrapped = SteeredGemma2(ConstantSteerer(torch.ones(1, 3)), model=ToyModel())
+    wrapped = SteeredCausalLM(ConstantSteerer(torch.ones(1, 3)), model=ToyModel())
     wrapped.attach_hook(layer=0, hook_point="post_mlp")
     assert wrapped.classify_layer == 0
     assert wrapped.steer_layer == 0
