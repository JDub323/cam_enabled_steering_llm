# cam_enabled_steering_llm
LLM's exhibit many malicious behaviors which can be detected early in the generation of a new token. If detected, one can steer LLM internal activations away from malicious behavior, potentially avoiding it altogether while remaining steady in performance and minimizing the expensive post-training requirement. CAM is useful for this end.
